create table company (
  id INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
  short_name varchar(100),
  name varchar(100),
  phone varchar(100),
  phone2 timestamp,
  email varchar(100),
  address varchar(100),
  orgcode varchar(100)
);
